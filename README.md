# 📄 PDF Normocontrol Checker

Автоматизированная система проверки оформления отчетов и документов.

---

## 👥 Команда проекта

Распределение задач и ответственности:

### **[Имя Фамилия]** — Layout & Geometry Engineer
- **Задачи:** Реализация алгоритмов проверки геометрических параметров страницы (поля, абзацные отступы). Работа с координатами текста.

### **[Имя Фамилия]** — Media & References Specialist
- **Задачи:** Анализ графических объектов. Поиск рисунков, подписей к ним и валидация перекрестных ссылок в тексте.

### **[Имя Фамилия]** — Structures Developer (Tables & Lists)
- **Задачи:** Сложный парсинг таблиц (границы, переносы) и списков (маркеры, структура).

### **[Имя Фамилия]** — Backend Lead & Integrator
- **Задачи:** Разработка архитектуры API (FastAPI), сборка общего отчета об ошибках, проверка библиографии, интеграция модулей.

---

## 🚀 Функциональность

Система выполняет **7 видов проверок**:

1. **Поля страницы** — Проверка отступов (слева, справа, сверху, снизу)
2. **Рисунки** — Проверка наличия подписей и нумерации
3. **Ссылки на рисунки** — Поиск упоминаний рисунков в тексте (перекрестные ссылки)
4. **Библиография** — Проверка порядка упоминания источников литературы (`[1]`, `[2]`...)
5. **Таблицы** — Проверка корректности оформления и переноса таблиц
6. **Списки** — Проверка маркеров и пробелов после них
7. **Абзацные отступы** — Проверка единообразия «красной строки»

---

## 🛠 Технический стек

- **Язык:** Python 3.9+
- **Web Framework:** FastAPI
- **PDF Engine:** pdfplumber
- **Frontend:** HTML5 + Bootstrap (Jinja2 Templates)

---

## 📥 Инструкция по установке и запуску

Следуйте этим шагам, чтобы развернуть проект на локальном компьютере.

### 1. Клонирование репозитория

Откройте терминал (командную строку) и выполните:

```bash
git clone <ссылка_на_ваш_репозиторий.git>
cd <название_папки_проекта>
```

### 2. Создание виртуального окружения

Рекомендуется использовать виртуальное окружение, чтобы не засорять систему.

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

**macOS / Linux:**
```bash
python3 -m venv venv
source venv/bin/activate
```

### 3. Установка зависимостей

Установите необходимые библиотеки из файла `requirements.txt`:

```bash
pip install -r requirements.txt
```

### 4. Запуск сервера

Запустите веб-приложение:

```bash
uvicorn app:app --reload
```

> Флаг `--reload` позволяет серверу автоматически перезагружаться при изменении кода.

### 5. Использование

1. Откройте браузер и перейдите по адресу: **http://127.0.0.1:8000**
2. Нажмите кнопку **"Выберите файл"** и загрузите PDF-документ
3. Нажмите **"Проверить документ"**
4. Изучите отчет об ошибках

---

## 📂 Структура проекта

```
.
├── app.py             # Точка входа (Web-сервер FastAPI)
├── checker.py         # Логика проверок (бизнес-логика)
├── parser.py          # Модуль чтения и разбора PDF
├── config.py          # Настройки (размеры полей, паттерны regex)
├── requirements.txt   # Список зависимостей
├── templates/         # HTML-шаблоны сайта
│   └── index.html
└── README.md          # Документация
```

---
